# Gameplay Test Stratification

> Historical, non-canonical note.
> This file is no longer generated by `Tools/generate_gameplay_test_stratification.py` and is not part of the active governance chain.

## Current status

- Active runner/governance entrypoint remains `./run_tests.sh`.
- Active governance checks come from:
  - `python3 Tools/check_gameplay_test_stratification.py --mode strict`
  - `./run_tests.sh core`
  - `./run_tests.sh full`
  - touched-cluster readout
  - grep gates for removed IR/scaffolding vocabulary
- The persisted manifest is now a runner-selection cache only.
  - Per-test fields are limited to `fullyQualifiedName`, `category`, and `mode`.
- `Docs/Testing/Full-EditMode-Baseline-2026-04-13.md` is the pinned validation baseline.
- `Docs/Architecture/ADR/ADR-001-Tick-Boundary-and-IR-Visibility.md` records sequencing decisions and remaining cleanup scope.

## Why this document remains

- Older PRs and notes may still link to this path.
- Keeping a small note here is safer than deleting the file while historical references still exist.
- If the remaining manifest-free runner work lands, this document can move to archive in a later docs-only step.
